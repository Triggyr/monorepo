{
   "name": "@triggyr/http",
   "version": "1.0.0",
   "description": "Simiplified HTTP module built on axios",
   "main": "dist/index.js",
   "types": "dist/index.d.ts",
   "files": [
      "dist"
   ],
   "scripts": {
      "build": "tsc -p tsconfig.json",
      "clean": "rm -rf dist",
      "lint": "eslint src",
      "test": "vitest run"
   },
   "packageManager": "pnpm@10.14.0",
   "dependencies": {
      "axios": "^1.12.2"
   }
}
